/**
 * Default content for a new GTM Strategy document.
 *
 * Returns an empty string so the document starts blank.
 * The AI writes sections incrementally via `update_strategy_section` tool
 * calls, and each section appears in real-time via SSE `section_update` events.
 */

export const STRATEGY_TEMPLATE = ''
